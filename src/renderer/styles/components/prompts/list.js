import base from '../prompt-form';

const itemHeight = 16;

export default {
  list: {
    display: 'flex',
    flexDirection: 'column',
    margin: '35px 0px'
  },
  listItem: {
    height: itemHeight,
    margin: `12px ${base.margin}px`
  }
};

